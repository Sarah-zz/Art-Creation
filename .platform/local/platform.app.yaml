# .platform.app.yaml
name: art-creation
type: php:8.2

dependencies:
  php:
    composer/composer: '^2'

relationships:
  mysql: 'mysql:mysql'

web:
  locations:
    '/':
      root: 'public'
      index: ['index.php']
      passthru: '/index.php'

disk: 2048
