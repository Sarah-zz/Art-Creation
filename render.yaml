services:
  - type: web
    name: artcreation
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGO_URL
        sync: false
      - key: MONGO_APP_DB
        sync: false